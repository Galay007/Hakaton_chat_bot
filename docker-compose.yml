version: "3.9"

services:
  bot:
    build: .
    restart: unless-stopped
    environment:
      - BOT_TOKEN=${BOT_TOKEN}
      - MAX_SIZE=${MAX_SIZE}
      - LOG_LEVEL=INFO
    command: python -m src.main
